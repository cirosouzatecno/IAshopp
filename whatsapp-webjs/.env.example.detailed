# Configuração do Serviço WhatsApp Web.js
# Copiar este arquivo para .env e configurar as variáveis

# ===== CONFIGURAÇÃO DO SERVIDOR =====
# Porta em que o serviço Node.js vai rodar
# Certifique-se que a porta não está em uso por outro serviço
PORT=3001

# ===== INTEGRAÇÃO COM LARAVEL =====
# URL do webhook do Laravel que receberá as mensagens do WhatsApp
# Este endpoint recebe POST com as mensagens recebidas
LARAVEL_WEBHOOK_URL=http://127.0.0.1:8000/webhook/whatsapp-web

# Token de segurança para validar requisições do webhook
# Se definido, será enviado no header X-Webhook-Token
# Deixe vazio se não quiser usar autenticação
WEBHOOK_TOKEN=

# ===== AUTENTICAÇÃO DO WHATSAPP =====
# Pasta onde será armazenada a sessão de autenticação do WhatsApp
# A sessão é reutilizada para evitar escanear QR novamente
# Para forçar novo QR, delete esta pasta
SESSION_PATH=.wwebjs_auth

# ===== CONFIGURAÇÃO DO NODE_ENV =====
# Ambiente de execução (development, production, test)
NODE_ENV=development

# ===== OPCOES AVANÇADAS DO PUPPETEER =====
# Comentado: Use para debug ou ambientes especiais
# PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
# PUPPETEER_SKIP_VALIDATION=true

# ===== PROXY (OPCIONAL) =====
# Use se precisar conectar através de proxy
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=https://proxy.example.com:8080
